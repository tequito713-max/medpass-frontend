# CSS organizado

El archivo activo sigue siendo `global.css`, porque `main.jsx` ya lo importaba.
Ahora `global.css` solo importa los archivos CSS separados por seccion.

`global.original.css` es el respaldo del CSS original completo.
